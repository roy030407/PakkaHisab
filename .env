# Prisma reads this file via prisma.config.ts.
# Real credentials go in .env.local (gitignored) — NOT here.
# This file is committed to git — keep it placeholder-only.

DATABASE_URL="postgresql://placeholder"
