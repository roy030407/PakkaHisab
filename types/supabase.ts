/**
 * FILE: types/supabase.ts
 *
 * WHAT THIS DOES:
 *   Supabase database type definitions. This is a placeholder that enables
 *   compilation. Replace with generated output once the Supabase project
 *   is connected:
 *     npx supabase gen types typescript --linked > types/supabase.ts
 *
 * CHANGES THIS SESSION:
 *   - Placeholder created (any-typed to unblock compilation)
 *
 * WHERE IT FITS:
 *   Imported by lib/supabase/server.ts and lib/supabase/browser.ts.
 *   Supabase's complex template-literal type inference requires the
 *   generated output — this placeholder disables that inference until
 *   a real project is wired up.
 *
 * CALLED BY / IMPORTS FROM:
 *   lib/supabase/server.ts, lib/supabase/browser.ts
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
